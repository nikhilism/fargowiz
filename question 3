
#include <iostream>

using namespace std;

int main()
{
    //input
    int n, a[n], count = 0, b;
    cin >> n;
    
    //input for the array
    for(int i= 0; i <= n; i++)
    {
        cin>>a[i];
    }
    
    // searching for any 2 nums in the array to be equal and then printing it out
    for(int i= 0; i <= n; i++)
    {
        for(int j = i; j > 0; j--)
        {
            if(a[i] == a[j]) cout<<a[i];
        }
    }
    
}
